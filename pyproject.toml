[tool.poetry]
name = "cstar-cli"
version = "0.0.1"
description = "Cstar Command Line Interface"
authors = [
	"Giovanni Mancini <giovanni.mancini@pagopa.it>",
	"Alberto Re <alberto.re@pagopa.it>"
]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
psycopg2-binary = "^2.9.2"
pandas = "^1.3.4"
python-gnupg = "^0.4.8"

[tool.poetry.dev-dependencies]
black = "^21.12b0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
